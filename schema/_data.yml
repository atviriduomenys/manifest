---
type: object
title: Data description
patternProperties:
  "^[a-zA-ZąčęėįšųūžĄČĘĖĮŠŲŪŽ/_]+$":
    type: object
    title: Data object
    properties:
      source:
        $ref: _source.yml
      since:
        type: string
        format: date
      until:
        type: string
        format: date
      stars:
        enum:
          - 0
          - 1
          - 2
          - 3
          - 4
          - 5
      local:
        type: boolean
        title: Local object
        description: >
          Local objects does not have to be defined in the global vocabulary.
          Local objects must be specific to a specific dataset.
      identifier:
        type: array
        items:
          type: object
          properties:
            source:
              $ref: _source.yml
      properties:
        type: object
        patternProperties:
          "^[a-zA-ZąčęėįšųūžĄČĘĖĮŠŲŪŽ/_]+$":
            type: [object, "null"]
            title: Data field
            properties:
              type:
                type: string
                enum:
                  - string
                  - ref
                description: >
                  Kai kurie objektai, gali būti kompleksiniai, rodantys į kitą
                  objektą, šis laukas nurodo ar reikalinga nuoroda į kitą
                  objektą ar tiesiog tekstinė to objekto išraiška.
              since:
                type: string
                format: date
              until:
                type: string
                format: date
              stars:
                enum:
                  - 0
                  - 1
                  - 2
                  - 3
                  - 4
                  - 5
              source:
                $ref: _source.yml
              local:
                type: boolean
                title: Local property
                description: >
                  Local property does not have to be defined in the global
                  vocabulary.  Local property must be specific to a specific
                  dataset, for example an identifier used only for that
                  dataset.
              enum:
                type: array
            additionalProperties: false
        additionalProperties: false
    additionalProperties: false
additionalProperties: false
